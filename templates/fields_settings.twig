{% block fields_settings %}

    <div id="settings_div" class="wrap">
        <h2>{{ __( 'Engaging Networks Fields Settings', 'planet4-gutenberg-engagingnetworks' ) }}</h2>
        <p>{{ __( 'In this page you can define engaging networks fields that you want to be used for the forms.', 'planet4-gutenberg-engagingnetworks' ) }}</p>
    </div>
    <div class="wrap">
        <div id="en_settings_notices"></div>
    </div>
    <br>
    <div id="existing-fields-div" class="wrap"></div>
    <div class="clear"></div>

{% endblock %}