{% extends "base.twig" %}

{% block content %}

	<div class="clearfix"></div>
	<div class="skewed-overlay"></div>

	<article id="post-{{ post.ID }}">

		<header class="page-header">
			<div class="page-header-background">
				{#<img src="{{ background_image }}" class="page-header-image" alt="Image alt data"/>#}
			</div>
			<div class="container">
				<div class="top-page-tags">
					<a class="tag-item tag-item--main" href="#">{{ __('post') }}</a>
					{% for tag in post.tags %}
						<a class="tag-item " href="{{ tag.link }}">#{{ tag.title }}</a>
					{% endfor %}
				</div>
				<h1 class="page-header-title">{{ post.title|raw }}</h1>
				<div class="single-post-meta">
					{% if ( author_override ) %}
						{% set author_name = author_override %}
						{% set author_url = "#" %}
					{% else %}
						{% set author_name = post.author.name %}
						{% set author_url = post.author.path %}
					{% endif %}
					<address class="single-post-author">{{ __( 'by', 'planet4-master-theme' ) }}&nbsp;<a href="{{ author_url }}">{{ author_name }}</a></address>
					<time class="single-post-time" pubdate>{{ post.post_date|date }}</time>
				</div>
			</div>
		</header>

		<!-- Post Block Start -->
		<div class="container">
			<div class="post-content">
				<div class="row">
					<div class="col-lg-6 post-content-lead">
						{% if ( post.first_paragraph and post.take_action_page) %}
							<p>
								{{ post.first_paragraph|raw }}
							</p>
							{% if ( post.second_paragraph ) %}
								<p>
									{{ post.second_paragraph|raw }}
								</p>
							{% endif %}
						{% endif %}
					</div>
					{% if ( post.first_paragraph and post.take_action_page) %}
						<div class="col-lg-5 offset-lg-1">
							{% if ( post.take_action_boxout ) %}
								{{ post.take_action_boxout|shortcodes|raw }}
							{% endif %}
						</div>
					{% endif %}
				</div>
				<div class="row">
					<div class="col-lg-12">
						<div class="post-details">
							{{ post.content|e('wp_kses_post')|raw }}
						</div>
					</div>
				</div>
			</div>
		</div>
		<!-- Post Page Block End -->

		{% if ( author_override == '' and post.author.description ) %}
			{% include "blocks/author_profile.twig" with {post:post} %}
		{% endif %}

		<section class="article-listing page-section">
			{% if ( post.articles ) %}
				{{ post.articles|shortcodes|raw }}
			{% endif %}
		</section>
		{% if ( 'closed' != post.comment_status ) %}
			{% include "comments_section.twig" with {comments:post.get_comments()} %}
		{% endif %}
	</article>
{% endblock %}
