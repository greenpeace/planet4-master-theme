{% extends "base.twig" %}

{% block content %}
	{{ post.content|raw }}
{% if ( background_image ) %}
	<style>
	@media (min-width: 1200px) {
		.hero-img-wrap {
			background-image: url("{{ background_image }}");
		}
	}
	</style>
{% endif %}
{% endblock %}
